apiVersion: v1
kind: ConfigMap
metadata:
  name: sql-validator-config
  namespace: default
data:
  # Database configuration
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "academic_db"

  # Application configuration
  APP_HOST: "0.0.0.0"
  APP_PORT: "8000"

  # Logging
  LOG_LEVEL: "info"
