---
# Default namespace (already exists, but including for completeness)
apiVersion: v1
kind: Namespace
metadata:
  name: default

---
# Nexus namespace for monitoring stack
apiVersion: v1
kind: Namespace
metadata:
  name: nexus
  labels:
    name: nexus
    purpose: microservices
