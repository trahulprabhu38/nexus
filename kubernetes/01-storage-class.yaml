---
# StorageClass for local storage on VPS/bare metal servers
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-storage
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Retain
allowVolumeExpansion: true

---
# Alternative: Use Rancher's local-path-provisioner for automatic provisioning
# Uncomment this section if you want automatic provisioning
# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: local-path
#   annotations:
#     storageclass.kubernetes.io/is-default-class: "true"
# provisioner: rancher.io/local-path
# volumeBindingMode: WaitForFirstConsumer
# reclaimPolicy: Delete
